# OfficeAddInSandbox
Word JS AddIn Sandbox for testing code snippets 

## Search and Select
- Run the project
- Type `=rand()` and hit `enter` in Word to generate dummy text, or insert some text containing the word "video" 
- Click "Find ‘Video’"
- A list of results is generated
- Click on an item in the list to select the range in Word
